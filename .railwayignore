# Never exclude application source: Dockerfile builds the Afluence Miro web and
# admin bundles from this repository before layering them into the server image.
# Docker's own .dockerignore handles local artifacts and keeps the build context
# lean.

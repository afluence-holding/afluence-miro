# The application image is published and maintained by the AFFiNE project.
# Keep deployments on the stable channel; upgrades are applied deliberately
# through this repository rather than rebuilding the whole monorepo on Railway.
FROM ghcr.io/toeverything/affine:stable
